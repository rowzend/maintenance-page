services:
  maintenance-app:
    image: ghcr.io/${GITHUB_USERNAME}/maintenance-page:latest
    container_name: maintenance-app
    ports:
      - "3099:3000"
    environment:
      - NODE_ENV=production
    networks:
      - proxy-network
    restart: unless-stopped

networks:
  proxy-network:
    external: true
